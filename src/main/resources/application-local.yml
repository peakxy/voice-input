spring:
  datasource:
    url: jdbc:mysql://localhost:13306/voice_input?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 16379

logging:
  level:
    root: info
    cn.peakxy.input: debug
